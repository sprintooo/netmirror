# Google AdSense publisher ID (Auto Ads).
# Must be NEXT_PUBLIC_* so it is inlined into the client bundle at build time
# (including the `opennextjs-cloudflare build` step used for Cloudflare deploys).
NEXT_PUBLIC_ADSENSE_CLIENT=ca-pub-xxxxxxxxxxxxxxxx
